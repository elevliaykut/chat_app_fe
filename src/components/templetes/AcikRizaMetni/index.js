import React from "react";
import { connect } from "react-redux";
import AcikRizaMetniPageOrganisms from '../../organisms/AcikRizaMetni';

const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(AcikRizaMetniPageOrganisms);