import React from "react";
import { connect } from "react-redux";
import GizlilikSozlesmesiPageOrganisms from '../../organisms/GizlilikSozlesmesi';

const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(GizlilikSozlesmesiPageOrganisms);