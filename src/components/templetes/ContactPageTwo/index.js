import React from "react";
import { connect } from "react-redux";
import ContactPageTwoOrganisms from '../../organisms/ContactPageTwo';


const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(ContactPageTwoOrganisms);